beta(for 1.0){
	finish the main part of the game{
		add sounds;
		patch the bugs;
		
	};
 	make the android version;
 	add credits;
}

release(1.x){
	add a leaderboard;
}
beta(for 2.0){
	make the installer;
	make the ai part of the game;
}
